type navProps = {
    title: string,
    path: string,
    icon: string
}


type dashboardProps = {
    // role: 'employee' | 'customer' | 'posp',
    // access:'admin' | 'user' | 'agent' | 'hr'
    // sideProps: navProps[]
}

export type { dashboardProps }